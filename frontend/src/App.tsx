import { ChatPage } from '@/pages/ChatPage'

// LOGIN: when sign-in exists, show <LoginPage /> (design/login.html) until the
// clinician is signed in — see frontend/src/features/auth/README.md.
export default function App() {
  return <ChatPage />
}
